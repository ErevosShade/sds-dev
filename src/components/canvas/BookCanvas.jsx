// TODO: CSS 3D + GSAP scroll-pinned flipping book
// Used in Speakers section ("The Reading List")
// One speaker per page flip, scroll progress drives rotateY

const BookCanvas = () => null;
export default BookCanvas;
